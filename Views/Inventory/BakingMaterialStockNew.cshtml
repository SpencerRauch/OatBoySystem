@model BakingMaterial
@{
    ViewData["Title"] = "Adding @Model.Name stock";
}
<div class="text-center container">

<h1>Stocking @Model.Name</h1>
@await Html.PartialAsync("_BakingMaterialRestock", new BakingMaterialStockForm(){BakingMaterial = Model})
</div>